using System;

class Program
{
    static void Main()
    {
        double length = 10;
        double breadth = 5;

        double perimeter = 2 * (length + breadth);
        Console.WriteLine("Perimeter = " + perimeter);
    }
}
